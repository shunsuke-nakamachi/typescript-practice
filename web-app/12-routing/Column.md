# Column: Chapter 12 & 13 の補足

## 詰まりそうなポイント：パスパラメータの型
`c.req.param('id')` で取得できる値は、たとえ数字であっても最初は「文字列」として返ってきます。計算に使いたい場合は `Number()` 等で数値に変換する必要があります。

## 豆知識：Hono Context の柔軟性
`c` (Context) オブジェクトは非常に強力で、変数を保持して次のミドルウェアに渡す (`c.set()` / `c.get()`) こともできます。これにより、認証情報などをハンドラーに安全に渡すことができます。
