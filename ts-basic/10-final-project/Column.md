# Column: Chapter 10 の補足

## 詰まりそうなポイント：型パズルの深入り
TypeScript は非常に強力で、型だけで複雑なロジックを組むことも可能ですが（型パズルと呼ばれます）、やりすぎるとコードの可読性が落ちます。「ビジネスロジックを助けるための型」であることを忘れないようにしましょう。

## 豆知識：`readonly` 配列
`const` で宣言した配列でも、中身を書き換えることはできてしまいます。
`readonly string[]` や `as const` を使うことで、中身の書き換えも禁止でき、より堅牢なプログラムになります。
